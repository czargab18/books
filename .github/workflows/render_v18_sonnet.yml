name: render-books
description: Renderiza livros Quarto e cria PR para revisão

on:
  push:
    branches: [main]
    paths: ["build/**/*.qmd", "build/**/*.yml", "build/*.bib"]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

env:
  USERNAME: ${{ github.repository_owner }}
  USEREMAIL: ${{ secrets.USEREMAIL }}
  TOKEN: ${{ secrets.TOKEN_REPO_SYNC }}

jobs:
  render-and-create-pr:
    runs-on: ubuntu-latest
    outputs:
      pull-request-number: ${{ steps.create-pr.outputs.pull-request-number }}
    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4
        with:
          token: ${{ env.TOKEN }}
          fetch-depth: 0

      - name: Configura Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Cria diretório de renderização
        run: |
          echo "📁 Criando diretório para livros renderizados..."
          mkdir -p _rendered
          echo "✅ Diretório _rendered criado"

      - name: Lista livros válidos
        id: list-books
        run: |
          echo "🔍 Buscando livros com padrão [A-Z]{3}[0-9]{4}..."
          
          # Usa regex mais limpa no find
          book_list=$(find build -maxdepth 1 -type d -regex '.*/[A-Z]\{3\}[0-9]\{4\}

      - name: Prepara estrutura para Pull Request
        run: |
          echo "🔄 Preparando arquivos para PR na branch principal..."
          
          # Cria/atualiza diretório book/ na própria main
          echo "🔄 Sincronizando livros renderizados na pasta book/..."
          
          # Remove versões antigas apenas dos livros que foram re-renderizados
          # (livros que seguem o padrão [A-Z]{3}[0-9]{4})
          for folder in $(ls _rendered 2>/dev/null || echo ""); do
            # Verifica se o nome segue o padrão antes de remover
            if echo "$folder" | grep -qE '^[A-Z]{3}[0-9]{4}

      - name: Gera índice dos livros (JSON)
        run: |
          echo "📋 Gerando índice de livros..."
          echo '{
            "books": [' > book/books-index.json
          
          first=true
          # Processa apenas livros que seguem o padrão [A-Z]{3}[0-9]{4}
          for folder in $(ls book 2>/dev/null | grep -E '^[A-Z]{3}[0-9]{4}

      - name: Cria Pull Request para branch padrão
        id: create-pr
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ env.TOKEN }}
          branch: books-render-$(date +%Y%m%d-%H%M%S)
          base: main  # ou master, dependendo da sua branch padrão
          title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
          body: |
            ## 📖 Nova Renderização dos Livros Quarto
            
            Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
            ### 📋 Detalhes da Renderização
            - **Commit fonte**: ${{ github.sha }}
            - **Workflow**: ${{ github.run_id }}
            - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
            - **Triggering commit**: ${{ github.event.head_commit.message }}
            
            ### 📚 Livros Renderizados
            $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
            ### 🔄 Mudanças Incluídas
            - ✅ Livros Quarto renderizados para HTML na pasta `book/`
            - ✅ Índice de livros atualizado (`book/books-index.json`)
            - ✅ Estrutura de arquivos organizada
            
            ### 📁 Estrutura Adicionada/Atualizada
            ```
            book/
            ├── books-index.json     ← Índice com metadata
            ├── livro1/
            │   ├── index.html
            │   └── assets/
            ├── livro2/
            │   ├── index.html
            │   └── assets/
            └── ...
            ```
            
            ### 📝 Arquivos Principais
            - `book/books-index.json` - Índice com metadata dos livros
            - `book/*/index.html` - Páginas principais dos livros
            - Assets e recursos complementares
            
            ---
            
            ## 👀 Review Checklist
            
            Por favor, verifique:
            - [ ] 📖 Livros foram renderizados corretamente
            - [ ] 🎨 Formatação e estilos estão adequados  
            - [ ] 🔗 Links internos funcionam
            - [ ] 📱 Layout é responsivo
            - [ ] 📋 Índice JSON está correto
            - [ ] 📁 Estrutura de pastas está organizada
            
            ---
            **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
          labels: |
            books-update
            quarto-render
            needs-review
            automated-pr
          assignees: ${{ github.repository_owner }}
          draft: false
          commit-message: |
            📚 Adiciona/Atualiza livros renderizados
            
            Livros processados:
            $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
            - Renderização Quarto → HTML
            - Índice JSON gerado automaticamente
            - Estrutura organizada em book/
            
            Commit fonte: ${{ github.sha }}
            Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

      - name: Adiciona comentário com instruções
        if: steps.create-pr.outputs.pull-request-number
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ env.TOKEN }}
          issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
          body: |
            ## 🤖 Instruções Automáticas
            
            ### 📋 Status do Workflow
            ✅ Livros renderizados com sucesso  
            ✅ Pull Request criada  
            ⏳ **Aguardando sua aprovação**  
            
            ### 🔍 Como Revisar
            
            1. **Verifique os arquivos alterados** na aba "Files changed"
            2. **Teste os livros localmente** se necessário:
               ```bash
               git checkout update-books-$(date +%Y%m%d-%H%M%S)
               # Abra book/*/index.html no navegador
               ```
            3. **Aprove a PR** quando estiver satisfeito
            
            ### 📁 Arquivos Importantes
            - `books-index.json` - Metadata dos livros
            - Cada pasta de livro contém o HTML renderizado
            
            ---
            *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

      - name: Limpa arquivos temporários
        run: |
          rm -rf _rendered
          echo "✅ Arquivos temporários removidos"

  summary:
    needs: [render-and-create-pr]
    runs-on: ubuntu-latest
    steps:
      - name: Resultado final
        run: |
          if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
            echo "🎉 Pipeline concluído com sucesso!"
            echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
            echo "⏳ Aguardando aprovação do proprietário do repositório"
            echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
          else
            echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
          fi; then
              if [ -d "book/$folder" ]; then
                echo "🗑️  Removendo versão antiga: $folder"
                rm -rf "book/$folder"
              else
                echo "📚 Novo livro detectado: $folder"
              fi
            else
              echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
            fi
          done
          
          # Move novos livros renderizados
          mkdir -p book
          if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
            cp -r _rendered/* book/
            echo "✅ Livros sincronizados em book/ com sucesso"
            
            # Lista livros sincronizados
            echo "📋 Livros sincronizados:"
            for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

      - name: Gera índice dos livros (JSON)
        run: |
          echo "📋 Gerando índice de livros..."
          echo '{
            "books": [' > book/books-index.json
          
          first=true
          for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
            if [ -d "book/$folder" ]; then
              if [ "$first" = false ]; then
                echo ',' >> book/books-index.json
              fi
              
              # Extrai título do index.html se existir
              title="$folder"
              if [ -f "book/$folder/index.html" ]; then
                extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
                if [ ! -z "$extracted_title" ]; then
                  title="$extracted_title"
                fi
              fi
              
              echo "    {
                \"id\": \"$folder\",
                \"title\": \"$title\",
                \"path\": \"$folder/index.html\",
                \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
              }" >> book/books-index.json
              first=false
            fi
          done
          
          echo '
            ],
            "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
          }' >> book/books-index.json
          
          echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

      - name: Cria Pull Request para branch padrão
        id: create-pr
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ env.TOKEN }}
          branch: books-render-$(date +%Y%m%d-%H%M%S)
          base: main  # ou master, dependendo da sua branch padrão
          title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
          body: |
            ## 📖 Nova Renderização dos Livros Quarto
            
            Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
            ### 📋 Detalhes da Renderização
            - **Commit fonte**: ${{ github.sha }}
            - **Workflow**: ${{ github.run_id }}
            - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
            - **Triggering commit**: ${{ github.event.head_commit.message }}
            
            ### 📚 Livros Renderizados
            $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
            ### 🔄 Mudanças Incluídas
            - ✅ Livros Quarto renderizados para HTML na pasta `book/`
            - ✅ Índice de livros atualizado (`book/books-index.json`)
            - ✅ Estrutura de arquivos organizada
            
            ### 📁 Estrutura Adicionada/Atualizada
            ```
            book/
            ├── books-index.json     ← Índice com metadata
            ├── livro1/
            │   ├── index.html
            │   └── assets/
            ├── livro2/
            │   ├── index.html
            │   └── assets/
            └── ...
            ```
            
            ### 📝 Arquivos Principais
            - `book/books-index.json` - Índice com metadata dos livros
            - `book/*/index.html` - Páginas principais dos livros
            - Assets e recursos complementares
            
            ---
            
            ## 👀 Review Checklist
            
            Por favor, verifique:
            - [ ] 📖 Livros foram renderizados corretamente
            - [ ] 🎨 Formatação e estilos estão adequados  
            - [ ] 🔗 Links internos funcionam
            - [ ] 📱 Layout é responsivo
            - [ ] 📋 Índice JSON está correto
            - [ ] 📁 Estrutura de pastas está organizada
            
            ---
            **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
          labels: |
            books-update
            quarto-render
            needs-review
            automated-pr
          assignees: ${{ github.repository_owner }}
          draft: false
          commit-message: |
            📚 Adiciona/Atualiza livros renderizados
            
            Livros processados:
            $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
            - Renderização Quarto → HTML
            - Índice JSON gerado automaticamente
            - Estrutura organizada em book/
            
            Commit fonte: ${{ github.sha }}
            Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

      - name: Adiciona comentário com instruções
        if: steps.create-pr.outputs.pull-request-number
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ env.TOKEN }}
          issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
          body: |
            ## 🤖 Instruções Automáticas
            
            ### 📋 Status do Workflow
            ✅ Livros renderizados com sucesso  
            ✅ Pull Request criada  
            ⏳ **Aguardando sua aprovação**  
            
            ### 🔍 Como Revisar
            
            1. **Verifique os arquivos alterados** na aba "Files changed"
            2. **Teste os livros localmente** se necessário:
               ```bash
               git checkout update-books-$(date +%Y%m%d-%H%M%S)
               # Abra book/*/index.html no navegador
               ```
            3. **Aprove a PR** quando estiver satisfeito
            
            ### 📁 Arquivos Importantes
            - `books-index.json` - Metadata dos livros
            - Cada pasta de livro contém o HTML renderizado
            
            ---
            *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

      - name: Limpa arquivos temporários
        run: |
          rm -rf _rendered
          echo "✅ Arquivos temporários removidos"




# Extra do Cloude Sonnet
  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi | sort); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             subject_code="${folder:0:3}"  # Primeiras 3 letras
  #             course_number="${folder:3:4}" # 4 números
              
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"subject_code\": \"$subject_code\",
  #               \"course_number\": \"$course_number\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
              
  #             echo "   📖 Indexado: $folder ($title)"
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         book_count=$(ls book 2>/dev/null | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi; then
  #             if [ -d "book/$folder" ]; then
  #               echo "🗑️  Removendo versão antiga: $folder"
  #               rm -rf "book/$folder"
  #             else
  #               echo "📚 Novo livro detectado: $folder"
  #             fi
  #           else
  #             echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
  #           fi
  #         done
          
  #         # Move novos livros renderizados
  #         mkdir -p book
  #         if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
  #           cp -r _rendered/* book/
  #           echo "✅ Livros sincronizados em book/ com sucesso"
            
  #           # Lista livros sincronizados
  #           echo "📋 Livros sincronizados:"
  #           for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi | wc -l)
  #         echo "✅ Índice gerado com $book_count livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi; then
  #             if [ -d "book/$folder" ]; then
  #               echo "🗑️  Removendo versão antiga: $folder"
  #               rm -rf "book/$folder"
  #             else
  #               echo "📚 Novo livro detectado: $folder"
  #             fi
  #           else
  #             echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
  #           fi
  #         done
          
  #         # Move novos livros renderizados
  #         mkdir -p book
  #         if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
  #           cp -r _rendered/* book/
  #           echo "✅ Livros sincronizados em book/ com sucesso"
            
  #           # Lista livros sincronizados
  #           echo "📋 Livros sincronizados:"
  #           for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi | sort)
  #         book_count=$(echo "$book_list" | wc -l)
          
  #         if [ -z "$book_list" ] || [ "$book_count" -eq 0 ]; then
  #           echo "❌ Nenhum livro encontrado com padrão [A-Z]{3}[0-9]{4}"
  #           echo "📁 Conteúdo atual de build/:"
  #           ls -la build/ 2>/dev/null || echo "Diretório build/ não existe"
  #           exit 1
  #         fi
          
  #         echo "📚 Livros encontrados ($book_count):"
  #         echo "$book_list" | while read folder; do
  #           book_name=$(basename "$folder")
  #           echo "   📖 $book_name"
  #         done
          
  #         # Salva lista para próximo step
  #         echo "$book_list" > book_list.txt
  #         echo "books_found=$book_count" >> $GITHUB_OUTPUT

  #     - name: Renderiza cada livro
  #       run: |
  #         echo "🔄 Iniciando renderização dos livros..."
          
  #         while read -r folder; do
  #           if [ -z "$folder" ]; then continue; fi
            
  #           book_name=$(basename "$folder")
  #           echo "⚡ Renderizando: $book_name"
            
  #           if [ -f "$folder/_quarto.yml" ] || [ -f "$folder/index.qmd" ]; then
  #             if quarto render "$folder" --to html --execute --output-dir "_rendered/$book_name"; then
  #               echo "✅ $book_name → Sucesso"
  #             else
  #               echo "❌ $book_name → Erro na renderização"
  #               exit 1
  #             fi
  #           else
  #             echo "⚠️  $book_name → Sem _quarto.yml ou index.qmd (pulando)"
  #           fi
  #         done < book_list.txt

  #     - name: Relatório de renderização
  #       run: |
  #         echo "📊 Relatório final da renderização:"
  #         echo "   📚 Livros processados: ${{ steps.list-books.outputs.books_found }}"
          
  #         rendered_count=$(ls _rendered 2>/dev/null | wc -l)
  #         echo "   ✅ Livros renderizados: $rendered_count"
          
  #         if [ "$rendered_count" -gt 0 ]; then
  #           echo "📋 Livros renderizados com sucesso:"
  #           ls _rendered | while read book; do
  #             echo "   🎯 $book"
  #           done
  #         else
  #           echo "❌ Nenhum livro foi renderizado"
  #           exit 1
  #         fi
          
  #         # Limpa arquivo temporário
  #         rm -f book_list.txt

  #     - name: Prepara estrutura para Pull Request
  #       run: |
  #         echo "🔄 Preparando arquivos para PR na branch principal..."
          
  #         # Cria/atualiza diretório book/ na própria main
  #         echo "🔄 Sincronizando livros renderizados na pasta book/..."
          
  #         # Remove versões antigas apenas dos livros que foram re-renderizados
  #         # (livros que seguem o padrão [A-Z]{3}[0-9]{4})
  #         for folder in $(ls _rendered 2>/dev/null || echo ""); do
  #           # Verifica se o nome segue o padrão antes de remover
  #           if echo "$folder" | grep -qE '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         # Processa apenas livros que seguem o padrão [A-Z]{3}[0-9]{4}
  #         for folder in $(ls book 2>/dev/null | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi; then
  #             if [ -d "book/$folder" ]; then
  #               echo "🗑️  Removendo versão antiga: $folder"
  #               rm -rf "book/$folder"
  #             else
  #               echo "📚 Novo livro detectado: $folder"
  #             fi
  #           else
  #             echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
  #           fi
  #         done
          
  #         # Move novos livros renderizados
  #         mkdir -p book
  #         if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
  #           cp -r _rendered/* book/
  #           echo "✅ Livros sincronizados em book/ com sucesso"
            
  #           # Lista livros sincronizados
  #           echo "📋 Livros sincronizados:"
  #           for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi | sort); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             subject_code="${folder:0:3}"  # Primeiras 3 letras
  #             course_number="${folder:3:4}" # 4 números
              
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"subject_code\": \"$subject_code\",
  #               \"course_number\": \"$course_number\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
              
  #             echo "   📖 Indexado: $folder ($title)"
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         book_count=$(ls book 2>/dev/null | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi; then
  #             if [ -d "book/$folder" ]; then
  #               echo "🗑️  Removendo versão antiga: $folder"
  #               rm -rf "book/$folder"
  #             else
  #               echo "📚 Novo livro detectado: $folder"
  #             fi
  #           else
  #             echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
  #           fi
  #         done
          
  #         # Move novos livros renderizados
  #         mkdir -p book
  #         if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
  #           cp -r _rendered/* book/
  #           echo "✅ Livros sincronizados em book/ com sucesso"
            
  #           # Lista livros sincronizados
  #           echo "📋 Livros sincronizados:"
  #           for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi | wc -l)
  #         echo "✅ Índice gerado com $book_count livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi; then
  #             if [ -d "book/$folder" ]; then
  #               echo "🗑️  Removendo versão antiga: $folder"
  #               rm -rf "book/$folder"
  #             else
  #               echo "📚 Novo livro detectado: $folder"
  #             fi
  #           else
  #             echo "⚠️  Pasta $folder não segue padrão [A-Z]{3}[0-9]{4} - ignorando"
  #           fi
  #         done
          
  #         # Move novos livros renderizados
  #         mkdir -p book
  #         if [ -d "_rendered" ] && [ "$(ls -A _rendered 2>/dev/null)" ]; then
  #           cp -r _rendered/* book/
  #           echo "✅ Livros sincronizados em book/ com sucesso"
            
  #           # Lista livros sincronizados
  #           echo "📋 Livros sincronizados:"
  #           for book in $(ls book/ | grep -E '^[A-Z]{3}[0-9]{4}

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi); do
  #             echo "   📖 $book"
  #           done
  #         else
  #           echo "⚠️  Nenhum livro foi renderizado"
  #           exit 1
  #         fi

  #     - name: Gera índice dos livros (JSON)
  #       run: |
  #         echo "📋 Gerando índice de livros..."
  #         echo '{
  #           "books": [' > book/books-index.json
          
  #         first=true
  #         for folder in $(ls book 2>/dev/null | grep -v "books-index.json" || echo ""); do
  #           if [ -d "book/$folder" ]; then
  #             if [ "$first" = false ]; then
  #               echo ',' >> book/books-index.json
  #             fi
              
  #             # Extrai título do index.html se existir
  #             title="$folder"
  #             if [ -f "book/$folder/index.html" ]; then
  #               extracted_title=$(grep -o '<title[^>]*>[^<]*' "book/$folder/index.html" | sed 's/<title[^>]*>//' | head -1)
  #               if [ ! -z "$extracted_title" ]; then
  #                 title="$extracted_title"
  #               fi
  #             fi
              
  #             echo "    {
  #               \"id\": \"$folder\",
  #               \"title\": \"$title\",
  #               \"path\": \"$folder/index.html\",
  #               \"updated\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"
  #             }" >> book/books-index.json
  #             first=false
  #           fi
  #         done
          
  #         echo '
  #           ],
  #           "last_updated": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
  #         }' >> book/books-index.json
          
  #         echo "✅ Índice gerado: $(cat book/books-index.json | jq '.books | length') livros"

  #     - name: Cria Pull Request para branch padrão
  #       id: create-pr
  #       uses: peter-evans/create-pull-request@v5
  #       with:
  #         token: ${{ env.TOKEN }}
  #         branch: books-render-$(date +%Y%m%d-%H%M%S)
  #         base: main  # ou master, dependendo da sua branch padrão
  #         title: "📚 Atualização dos livros renderizados - $(date +'%Y-%m-%d %H:%M')"
  #         body: |
  #           ## 📖 Nova Renderização dos Livros Quarto
            
  #           Esta PR contém os livros atualizados renderizados a partir das mudanças na branch `main`.
            
  #           ### 📋 Detalhes da Renderização
  #           - **Commit fonte**: ${{ github.sha }}
  #           - **Workflow**: ${{ github.run_id }}
  #           - **Data**: $(date +'%Y-%m-%d %H:%M:%S UTC')
  #           - **Triggering commit**: ${{ github.event.head_commit.message }}
            
  #           ### 📚 Livros Renderizados
  #           $(ls _rendered 2>/dev/null | sed 's/^/- 📖 /' || echo "- Nenhum livro renderizado")
            
  #           ### 🔄 Mudanças Incluídas
  #           - ✅ Livros Quarto renderizados para HTML na pasta `book/`
  #           - ✅ Índice de livros atualizado (`book/books-index.json`)
  #           - ✅ Estrutura de arquivos organizada
            
  #           ### 📁 Estrutura Adicionada/Atualizada
  #           ```
  #           book/
  #           ├── books-index.json     ← Índice com metadata
  #           ├── livro1/
  #           │   ├── index.html
  #           │   └── assets/
  #           ├── livro2/
  #           │   ├── index.html
  #           │   └── assets/
  #           └── ...
  #           ```
            
  #           ### 📝 Arquivos Principais
  #           - `book/books-index.json` - Índice com metadata dos livros
  #           - `book/*/index.html` - Páginas principais dos livros
  #           - Assets e recursos complementares
            
  #           ---
            
  #           ## 👀 Review Checklist
            
  #           Por favor, verifique:
  #           - [ ] 📖 Livros foram renderizados corretamente
  #           - [ ] 🎨 Formatação e estilos estão adequados  
  #           - [ ] 🔗 Links internos funcionam
  #           - [ ] 📱 Layout é responsivo
  #           - [ ] 📋 Índice JSON está correto
  #           - [ ] 📁 Estrutura de pastas está organizada
            
  #           ---
  #           **⚠️ Esta PR requer aprovação manual do proprietário antes do merge.**
  #         labels: |
  #           books-update
  #           quarto-render
  #           needs-review
  #           automated-pr
  #         assignees: ${{ github.repository_owner }}
  #         draft: false
  #         commit-message: |
  #           📚 Adiciona/Atualiza livros renderizados
            
  #           Livros processados:
  #           $(ls _rendered 2>/dev/null | sed 's/^/- /' || echo "- Nenhum")
            
  #           - Renderização Quarto → HTML
  #           - Índice JSON gerado automaticamente
  #           - Estrutura organizada em book/
            
  #           Commit fonte: ${{ github.sha }}
  #           Data: $(date +'%Y-%m-%d %H:%M:%S UTC')

  #     - name: Adiciona comentário com instruções
  #       if: steps.create-pr.outputs.pull-request-number
  #       uses: peter-evans/create-or-update-comment@v3
  #       with:
  #         token: ${{ env.TOKEN }}
  #         issue-number: ${{ steps.create-pr.outputs.pull-request-number }}
  #         body: |
  #           ## 🤖 Instruções Automáticas
            
  #           ### 📋 Status do Workflow
  #           ✅ Livros renderizados com sucesso  
  #           ✅ Pull Request criada  
  #           ⏳ **Aguardando sua aprovação**  
            
  #           ### 🔍 Como Revisar
            
  #           1. **Verifique os arquivos alterados** na aba "Files changed"
  #           2. **Teste os livros localmente** se necessário:
  #              ```bash
  #              git checkout update-books-$(date +%Y%m%d-%H%M%S)
  #              # Abra book/*/index.html no navegador
  #              ```
  #           3. **Aprove a PR** quando estiver satisfeito
            
  #           ### 📁 Arquivos Importantes
  #           - `books-index.json` - Metadata dos livros
  #           - Cada pasta de livro contém o HTML renderizado
            
  #           ---
  #           *Esta PR foi criada automaticamente e **requer sua aprovação** para merge.*

  #     - name: Limpa arquivos temporários
  #       run: |
  #         rm -rf _rendered
  #         echo "✅ Arquivos temporários removidos"

  # summary:
  #   needs: [render-and-create-pr]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Resultado final
  #       run: |
  #         if [ "${{ needs.render-and-create-pr.outputs.pull-request-number }}" ]; then
  #           echo "🎉 Pipeline concluído com sucesso!"
  #           echo "📋 Pull Request criada: #${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #           echo "⏳ Aguardando aprovação do proprietário do repositório"
  #           echo "👀 Revisar em: ${{ github.server_url }}/${{ github.repository }}/pull/${{ needs.render-and-create-pr.outputs.pull-request-number }}"
  #         else
  #           echo "ℹ️  Nenhuma PR foi criada (possivelmente sem mudanças)"
  #         fi